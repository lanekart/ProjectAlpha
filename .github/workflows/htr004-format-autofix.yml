name: HTR-004 Format Autofix

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/htr-004-canonical-replay

      - name: Install locked Ruff
        run: pip install ruff==0.15.20

      - name: Format and verify repository
        run: |
          ruff format .
          ruff check .
          ruff format --check .

      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -u
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Apply repository Ruff formatting"
          git push origin HEAD:feature/htr-004-canonical-replay
